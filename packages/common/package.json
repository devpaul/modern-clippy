{
  "name": "modern-clippy-common",
  "version": "1.0.0",
  "description": "Common files across implementations",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:esm && npm run build:cjs",
    "build:esm": "tsc",
    "build:cjs": "tsc -p ./tsconfig.cjs.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paul Shannon",
  "license": "MPL-2.0",
  "devDependencies": {
    "typescript": "^3.5.2"
  }
}
